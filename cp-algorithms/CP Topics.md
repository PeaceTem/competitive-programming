# ✅ **Classic CP Questions (Huge List)**

### Finish everything here by February, then focus on all dp types and other DS
## **1. Dynamic Programming (DP)** [MUST DO 25]

**Sequence DP**

- Longest Common Subsequence (LCS) `DONE`
    
- Longest Increasing Subsequence (LIS)  `DONE`
    
- Longest Decreasing Subsequence (LDS) `DONE`
    
- Edit Distance (10)
    
- Minimum Insertions/Deletions to make strings equal (9)
    
- Maximum Subarray Sum (Kadane) `DONE`
    
- Maximum Sum Increasing Subsequence (8)
    
- Count subsequences equal to a target string (7)
    
- Palindromic Subsequences count (6)
    
- Longest Palindromic Subsequence (LPS) (5)
    
- Longest Palindromic Substring (5)
    

**DP on Grids**

- Unique Paths `DONE`
    
- Minimum path sum
    
- Cherry Pickup
    
- DP on 2D grid with obstacles
    
- Count paths in a maze
    

**Knapsack Variants**

- 0/1 Knapsack
    
- Unbounded Knapsack
    
- Bounded Knapsack
    
- Subset sum
    
- Partition equal subset
    
- Coin change (count ways / min coins)
    

**DP on Trees**

- Tree diameter (1) `DONE`
    
- DP for subtree sizes (2) `DONE`
    
- Maximum path sum in tree (3) `DONE`
    
- DP on rooted trees (rerooting) (4)
    

**Bitmask DP**

- Traveling Salesman Problem (TSP)
    
- Count ways to assign jobs to people
    
- Grid DP with bitmask
    

---

## **2. Binary Search Problems** [MUST DO AT LEAST 6]

- Median of two sorted arrays
    
- K-th smallest number in sorted matrices
    
- Minimum capacity to ship packages `DONE`
    
- Allocate pages / Painters partition `DONE`
    
- Search in rotated sorted array `DONE`
    
- Find first/last occurrence in sorted array `DONE`
    
- Square root / nth root using binary search `DONE`
    
- Maximize minimum distance (aggressive cows) `DONE`
    

---

## **3. Greedy Algorithms** [MUST DO AT LEAST 2]

- Activity/interval scheduling `DONE`
    
- Fractional knapsack
    
- Huffman coding
    
- Job sequencing with deadlines
    
- Minimum platforms for trains
    
- Gas station circular tour
    
- Meeting rooms / merging intervals
    
- Min arrows to burst balloons `DONE`
    

---

## **4. Sliding Window / Two Pointers** [MUST DO 7]

- Longest substring without repeating characters `DONE`
    
- Minimum window substring `DONE`
    
- Count subarrays with sum = K `DONE`
    
- Subarray with product < K `DONE`
    
- Trapping rain water
    
- Container with most water
    
- Three-sum / Four-sum `DONE`
    

---

## **5. Graph Algorithms** [MUST DO 18]

**Shortest Paths**

- Dijkstra (7)
    
- Bellman–Ford (8)
    
- Floyd–Warshall (9)
    
- 0-1 BFS (2) 
    
- Multi-source BFS (3)

start with the simplest one among these.

**Graph Traversal**

- BFS/DFS basics `DONE`
    
- Connected components `DONE`
    
- Cycle detection (undirected) `DONE` 
    
- Bipartite check (1)
- Inorder traversal `DONE`
- Postorder traversal `DONE`
- Preorder traversal `DONE`
    

**Tree Problems**

- LCA with binary lifting
    
- Euler tour `DONE`
    
- Subtree queries `DONE`
    
- Tree diameter `DONE`
    
- Counting paths in tree 
    

**Topological Problems**

- Topological sort (4)
    
- Course schedule (5)
    
- Detect cycle in directed graph (6)
    

**Minimum Spanning Tree (MST)**

- Kruskal `DONE`
    
- Prim `DONE`
    

**Flow / Matching**

- Maximum bipartite matching
    
- Max flow (Ford-Fulkerson / Edmonds-Karp / Dinic)
    
- Minimum cut
    
- Assignment problem
    

---

## **6. String Algorithms** [MUST DO 4]

- KMP pattern matching
    
- Z-algorithm
    
- Rabin-Karp
    
- Trie operations
    
- Minimum window substring `DONE`
    
- Rolling hash
    
- Suffix array
    
- Suffix tree / LCP array
    
- Anagram groups
    
- Longest repeated substring
    
- Palindrome check using hashing
    

---

## **7. Number Theory** [MUST DO 12]

- Sieve of Eratosthenes `DONE`
    
- Segmented sieve (5)
    
- Prime factorization `DONE`
    
- GCD / Extended Euclid `DONE`
    
- Modular inverse `DONE`
    
- Modular exponentiation `DONE`
    
- Chinese Remainder Theorem
    
- Euler totient
    
- Fast Fibonacci (matrix expo) (1)
    
- Count divisors / sum divisors `DONE`

- Modular arithmetic `DONE`
    
- Fermat’s little theorem `DONE`
    
- Prefix gcd (2) `DONE`
    
- Matrix exponentiation (3)
    
- Fibonacci fast doubling (4)
    
- Catalan numbers
    
- Inclusion–exclusion
    
- Diophantine equations
    
- Pell's equation (rare but classic)
- Generating Functions

---

## **8. Sorting & Order Statistics** [MUST DO 3]

- Kth largest using Quickselect
    
- Count inversions using merge sort (1)
    
- Sort by frequency `DONE`
    
- Check if sorting possible with swaps/constraints
    
- Merge intervals
    
- Smallest range covering K lists
    
- Median maintenance using heaps
    

---

## **9. Data Structures** [MUST DO 13] 

**Stacks / Queues**

- Next greater element `DONE`
    
- Next smaller element `DONE`
    
- Largest rectangle in histogram
    
- Evaluate RPN expression
    
- Min stack
    
- Sliding window maximum (3)
    

**Heaps/Priority Queue**

- K closest elements
    
- Merge K sorted lists
    
- Frequent elements
    

**Segment Tree / Fenwick Tree** (Must practice this part - Don't waste too much time on things that will not come out)

- Range sum queries  `DONE`
    
- Range minimum/max queries `DONE`
    
- Lazy propagation (1)
    
- Point update `DONE` / range update (2)
    

**Disjoint Set Union (DSU)**

- Union-find `DONE`
    
- Connected components `DONE`
    
- Cycle detection `DONE`
    
- Kruskal MST `DONE`
    

---

## **10. Geometry** [MUST DO 2 SIMPLEST LAST] NO NEED

- Orientation of 3 points
    
- Check line segment intersection
    
- Convex hull (Graham scan / Monotone chain)
    
- Closest pair of points
    
- Area of polygon
    
- Point in polygon
    

---

## **11. Misc / Hard Classics** [MUST DO 5] DONE

- N-Queens
    
- Sudoku solver
    
- Backtracking subsets/permutations `DONE`
    
- Maximum rectangle in binary matrix `Make sure you solve these two problems` (2)
    
- Largest square of 1s in matrix (USE DSU) `Make sure you solve these two problems` (1)
    
- Rotate matrix `DONE`
    
- Spiral traversal `DONE`

---
# **12. Bit Manipulation — Topics** [MUST DO 14] DONE

- Bitwise AND / OR / XOR `DONE`
    
- Left & right shifting `DONE`
    
- Checking if a bit is set `DONE`
    
- Setting / clearing / toggling a bit `DONE`
    
- Counting set bits (popcount) `DONE`
    
- Checking power of two `DONE`
    
- Isolating lowest set bit (LSB) `DONE`
    
- Removing lowest set bit `DONE`
    
- Bitmasking `DONE`
    
- Subset generation using bitmasks `DONE`
    
- Swapping without temp variable `DONE`
    
- XOR tricks (missing number, single number) `DONE`
    
- Fast exponentiation using bits `DONE`
    
- Using `__builtin` functions `DONE`
    
- Gray code
    
- Trie for binary representation
    
- Bit DP 2 (maybe)
    
- Integer representation (2’s complement)

Learn all those x + y - 2(x & y) type shii

---
